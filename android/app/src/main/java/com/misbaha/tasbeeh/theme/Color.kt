package com.misbaha.tasbeeh.theme

import androidx.compose.ui.graphics.Color

/**
 * Design tokens mirror /docs/03-BRAND-IDENTITY.md §3 exactly — this file is the single
 * source of truth on the Android client and must stay in sync with the documented palette.
 */
object MisbahaColors {
    val PrimaryLight = Color(0xFF0B6E4F)
    val PrimaryDark = Color(0xFF1C9A6E)

    val Gold = Color(0xFFB8912F)
    val GoldDark = Color(0xFFD4AF37)

    val SurfaceLight = Color(0xFFFBF9F4)
    val SurfaceDark = Color(0xFF0E1512)
    val SurfaceElevatedLight = Color(0xFFFFFFFF)
    val SurfaceElevatedDark = Color(0xFF16211C)

    val TextPrimaryLight = Color(0xFF1A1F1D)
    val TextPrimaryDark = Color(0xFFF3F1EA)
    val TextSecondaryLight = Color(0xFF5C645F)
    val TextSecondaryDark = Color(0xFFA9B3AC)

    val ErrorLight = Color(0xFFB3261E)
    val ErrorDark = Color(0xFFE4635A)
}
