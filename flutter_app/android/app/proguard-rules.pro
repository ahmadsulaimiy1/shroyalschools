# Misbaha release ProGuard/R8 rules.
# Flutter's own gradle plugin already adds the required keep rules for the
# Flutter engine; add app-specific rules here only if a shrink-related crash
# is observed in a release build.
