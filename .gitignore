# Fetched font files, re-downloadable by build/bundle.py
build/.fontcache/

# Print bundles carry embedded fonts and are only an intermediate step to the
# PDFs, which are committed. The Express bundles ARE committed — they are the
# deliverable you hand to Adobe Express.
dist/*.print.html
